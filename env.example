# Database configuration
DB_HOST=localhost           # Database host (e.g., localhost)
DB_USER=root                # Database username
DB_PASS_LOCAL=              # Database password for local development (leave blank if no password)
DB_NAME=jokes_favorite_db   # Name of the database
DB_DIALECT=mysql            # Database dialect (e.g., mysql, postgres)
DB_PORT=3306                # Port number for the database

# API configuration
HOST=http://localhost:4000/ # Base URL for the API
